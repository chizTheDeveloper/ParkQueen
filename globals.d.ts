// Injected by vite.config.ts via `define`, from sentryRelease.ts's
// resolveAppRelease() — the exact same value passed to the Sentry Vite
// plugin's `release.name` at build time, so the two can never diverge.
declare const __APP_RELEASE__: string;
